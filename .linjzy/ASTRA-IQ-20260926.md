# Astra 官方客户端检测升级验证

任务：每 5 分钟检测启用渠道的 gpt-6-astra，推理强度 medium，糖果题正确答案 21；失败即时跳过路由，卡片底部纯色块可打开历史详情。

## 实现和维护

- 独立补丁：`patches/astra-iq-gate.patch`，基线 v1.0.0-rc.40 / `0aec08fee811ec6136828fda790551b49e410301`。应用完整补丁后与实现工作区逐文件比较一致。
- 真实官方 Codex CLI 0.157.1 随镜像安装，官方发布包固定摘要；每次调用使用临时目录、只读沙箱和一次性本地代理，只有 `/v1/responses` 的一次请求可访问指定渠道。
- 保留既有渠道转换、模型映射、medium 参数覆盖、检测日志和用量逻辑。CLI 只持有临时代理令牌，不持有上游密钥。
- 详情复用 `components/dialog.tsx`、`loading-state.tsx`、`error-state.tsx`、`ui/button.tsx` 和 `ui/tooltip.tsx`。离散历史色块需要独立点击目标，现有 Progress 不支持此行为，因此新增业务时间线组合组件。
- 历史列扩容为跨数据库 LongText，保留原数据；旧记录没有保存的耗时和 Token 数不会补造。

## 本地验证

- `bash .linjzy/bin/verify-release.sh /Users/linjzy/new-api-astra-iq go`：通过。
- `bash .linjzy/bin/verify-release.sh /Users/linjzy/new-api-astra-iq race`：通过。
- 最终代理验证追加后：`go test -race ./controller -run '^TestCustomAstraIQ' -count=1`：通过。
- `bun run test src/features/channels/components/__tests__`：10 个文件、208 项通过。
- `bun run typecheck`、修改文件 oxlint 与 oxfmt 检查、`bun run build`：通过。
- `python3 -m unittest discover -s .linjzy/tests -p 'test_*.py'`：11 项通过。
- 发布脚本及 CLI 安装脚本 shellcheck：通过。
- 桌面、390px 手机、暗色主题实测；点击历史结果、Escape 关闭和弹窗滚动正常，无横向溢出。生产控制台当前浏览器会话未登录，视觉验证基于同一组件的本地预览。

## 安全边界核对

参照 [OWASP ASVS 5.0](https://github.com/OWASP/ASVS/tree/v5.0.0_release/5.0/en)、[Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)、[Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)。本次局部检查覆盖 ASVS 7.2.1–7.2.3 的服务端验证、动态令牌和强随机性，以及 7.4.1 的失效后拒绝使用；不代表整站 ASVS 认证。

本地令牌通过 crypto/rand 生成，常量时间比较，仅绑定回环地址，最多存活单次检测的 2 分钟，拒绝空/错误令牌、错误方法和路径及重复使用；对应回归已通过。临时文件在调用完成后删除，不保存 CLI 原始错误输出。历史详情仅使用已有渠道读取权限，响应不包含渠道密钥或指纹。

## 渠道 1 排查

升级前多轮检测返回 HTTP 403，原因：上游仅允许 Codex 官方客户端。2026-09-26 在 179 直接执行官方 CLI，gpt-6-astra / medium 约 11.13 秒返回 21。随后将真实 CLI 纳入定时检测，并修复了该上游的流式结果兼容问题；最终部署后渠道 1 返回 21，通过检测，详见下方记录。

## 最终发布和部署

首次升级验证流水线：https://github.com/linjzy/new-api/actions/runs/36238921298

流式兼容修复流水线：https://github.com/linjzy/new-api/actions/runs/36239892140

生产数据库及 Compose 备份：`/opt/new-api/backups/astra-iq-official-20260926/`。

最终流水线成功。数据库版本 SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19，均运行新建表、旧表迁移、两次启动迁移、大于 64KB 的历史保存、内存和数据库路由跳过/恢复验证；命令为发布包 `verify-release.sh SOURCE go`，并追加 `race`。

首次升级镜像：`ghcr.io/linjzy/new-api@sha256:c1470395ec4f5dfcc4eb9e18b694403923cf7961a2f215566c36095f4e35e94a`。

首次升级源码：`custom/1.0.0-rc.40-custom-96802322a014-0aec08fee811`，提交 `2ba26e72fad687a9252b23c84d4550827c66772c`。

2026-09-26 19:38（北京时间）启用新镜像，容器健康。渠道 1 首轮自动检测 19:38:11 返回 21 / HTTP 200，耗时 8.806 秒，首字 7.763 秒；历史详情保存了 Codex CLI 0.157.1 与 /v1/responses。

首轮渠道 3 于 19:38:15 回答 21 通过；渠道 2 于 19:38:36 回答 29，状态 fail / wrong_answer，按规则排除。三条渠道消费日志均为 medium 和 /v1/responses。部署任务结束码 0，耗时 15 秒，维护门闩已解除，容器 healthy，公网 /api/status HTTP 200 / success=true。

临时独立 CLI 安装包和二进制已清理；正式 CLI 保存在发布镜像内。临时构建工作树、复制的验证文件和预览进程已清理，保留预览截图和验证日志。数据库备份及上一镜像保留以便回滚。

19:43:02 自然触发第二轮，距上一轮开始 303 秒，调度符合 5 分钟 + 轮询误差。现场发现渠道 1 的部分 Responses 流在 output_item.done 中完整给出 21，但 response.completed 的 output 为空；原解析器将其误判为异常。已先增加失败用例复现，再允许成功完成响应使用已完成的 assistant 消息；未完成流与 commentary 仍不能通过。真实 CLI 集成、判分与 race 回归均通过。

### 最终流式兼容修复

- 最终流水线 `36239892140` 状态 success，包含三种数据库、race、前端发布检查、官方 CLI 集成和镜像启动验证。
- 最终镜像：`ghcr.io/linjzy/new-api@sha256:135dcc507b4f09b06c417a9d8273f7c3cca9f53ead2c82ee5bdf29d9faa31e44`。
- 最终源码：`custom/1.0.0-rc.40-custom-6db358a862ab-0aec08fee811`，提交 `95196c54eda4571261f104b8d8c57180f4a8ecd9`。
- 最终补丁摘要：`b5726ed8c11abfb74a340c994e5d8e4d84e178b38d5c7b69f643fd002a5e9b8e`。
- 2026-09-26 19:55:52（北京时间）部署完成，任务退出码 0，耗时 14 秒；维护门闩已解除，容器 healthy，公网 `/api/status` 返回 HTTP 200 / success=true。

部署后立即通过正常任务队列触发一轮官方客户端检测，结果如下。渠道启用状态保持原值，检测结果来自真实请求，没有手工改写判定。

| 渠道 | 完成时间（北京时间） | 回答 | 判定 | HTTP | 总耗时 | 首字耗时 | 输入 / 输出 Token |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 / openai-nex | 19:56:02 | 21 | pass | 200 | 13.695 秒 | 12.373 秒 | 12836 / 326 |
| 2 / openai-bot | 19:56:43 | 29 | fail / wrong_answer | 200 | 55.244 秒 | 2.518 秒 | 12834 / 518 |
| 3 / openai-krill | 19:56:04 | 21 | pass | 200 | 15.706 秒 | 3.012 秒 | 8682 / 382 |

以上三条历史均记录 `Codex CLI 0.157.1` 和 `/v1/responses`，对应消费日志均记录 medium。检测任务最终状态 succeeded。渠道 1 本轮真实流仍是 `response.output_item.done` 给出 completed / final_answer / 21，而 `response.completed` 为 completed / output=[]；新解析逻辑正确取得已完成答案，并与官方 CLI 完成结果一致，确认该兼容修复在生产实际生效。

历史中的旧黄色结果继续保留，最新检测按每轮结果显示；渠道 2 在这一轮按既定答案 21 的规则暂不参与 gpt-6-astra 路由。

20:00:48 自动开始下一轮，距前一轮开始 302 秒。渠道 2 于 20:01:09 回答 21，自动恢复路由；渠道 3 于 20:00:57 回答 21。渠道 1 于 20:01:20 请求失败，耗时 31.664 秒；同一请求的服务端流处理日志为 `handler_stop` / `Upstream service temporarily unavailable`，只收到 1 条事件。这属于上游服务暂不可用，与此前的 HTTP 403 客户端限制和空 terminal output 解析问题不同；按既定规则记录黄色并跳过该渠道，未将异常结果改写为通过。

20:06:03 下一轮自动开始，20:06:34 成功结束。渠道 1 已恢复正常响应，HTTP 200，但回答 29，按预设答案 21 判为 fail / wrong_answer（红色），耗时 27.651 秒；渠道 2 回答 29，fail，耗时 30.950 秒；渠道 3 回答 21，pass，耗时 12.868 秒。三条记录仍为真实官方 CLI / Responses 检测。最终确认客户端限制和空 terminal output 的误判已修复，真实上游故障和错误答案仍如实记录并控制路由，旧历史不被覆盖。

## 同行布局与历史删除（后续调整）

检测色块移动到分组名称后，同一行显示，不增加卡片高度。复用原 GroupBadge、AstraIQStatus、Dialog、ConfirmDialog 和 Button，没有新增基础交互封装。

在同组件预览中对比有/无检测条的相同卡片：1280px 和 390px 视口下均高 230px，320px 视口下均高 250px；单分组、多分组、长名称、无分组、停用渠道均无横向溢出，色块和分组垂直对齐。分组最多使用 40% 行宽，长名称沿用省略及悬停完整名称，剩余空间显示检测色块。

详情新增“删除此次结果”和“删除全部结果”。全部范围经用户确认仅为当前渠道；确认框明确范围。旧记录删除不改变最新判定；最新记录被删除或清空后，当前状态变为 pending，旧通过记录不恢复路由资格，下一次检测生成新结果。

- `DELETE /api/channel/astra_iq/:id/:checked_at`：删除指定时间记录，不存在返回 404，无效标识返回 400。
- `DELETE /api/channel/astra_iq/:id`：清空当前渠道，重复清空可安全重试。
- 两接口复用 AdminAuth 和 ChannelWrite 服务端权限校验及现有操作审计；只有渠道读取权限的页面不展示删除操作。
- 写入与删除使用 GORM 事务和现有 lockForUpdate 跨数据库封装，避免定时写入使用删除前的历史副本；不变更数据库结构。
- 参照 [OWASP ASVS 5.0.0 V8](https://github.com/OWASP/ASVS/blob/v5.0.0_release/5.0/en/0x17-V8-Authorization.md) 的 8.1.1、8.2.1、8.2.2、8.3.1，以及 [Authorization Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)，复用既有身份校验和会话机制。此为本次新增端点局部核对，不代表整站认证。

本地 `go test -race ./controller ./router -run '^TestCustomAstraIQ' -count=1` 通过；前端相关 15 项测试、typecheck、oxlint、oxfmt 通过。浏览器验证取消不删除，单次删除 24→23，清空 23→0，其他渠道仍为 24；网络错误和业务错误均保留历史、展示失败信息并允许重试。完整补丁可应用到固定上游，28 个补丁文件与实现源码一致；发布工具 11 项测试通过。

发布流水线：https://github.com/linjzy/new-api/actions/runs/36241979880 ，最终状态 success。SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19 均通过新增的删除/清空/后续写入/路由撤销回归；CI 前端 18 项测试通过，后端 race、typecheck、lint、构建、镜像启动和官方 CLI 检查通过，发布脚本 shellcheck 通过。

2026-09-26 20:37:22（北京时间）部署完成，退出码 0，耗时 35 秒。新镜像 `ghcr.io/linjzy/new-api@sha256:e155af55d650c246511d7ed785cfe094d0553c74d8efd4255c36a1b98e0a50a6`，源码 `custom/1.0.0-rc.40-custom-c7f43625e3bc-0aec08fee811` / `54e78d3ec83e0400b820f9bb93d94cf36d0fe4b9`，补丁摘要 `7412eaee78b8f2c515e3a9f29b43354e17c31a33bea9816e50fd1b3597b82489`。

生产容器 healthy，维护门闩解除；本地和公网 `/api/status` 均 HTTP 200 / success=true，两个删除端点的未认证请求均 HTTP 401，镜像内 `codex --version` 为 0.157.1。真实删除行为在隔离数据库和组件预览验证。上线前数据库及 Compose 备份保留在 `/opt/new-api/backups/astra-iq-history-20260926/`，dump 8,102,281 字节且已通过 pg_restore --list 验证。临时预览进程和工作树内预览文件已清理，截图、日志与预览归档保留在本机。

## 检测设置与无结果放行（本轮调整）

渠道页标题在“最大重试”后新增“检测设置”入口，弹窗使用现有 Dialog、Form、Input、Switch、Button、LoadingState 和 ErrorState；新增组件只组合业务数据、权限和保存行为。设置项为“开始时间”“结束时间”“间隔（分钟）”“失败停用”，全局适用于 Astra 渠道；标题变化不影响卡片大小。

- 每天按北京时间 UTC+8 执行，支持跨午夜，相同时间为全天；开始边界包含、结束边界不包含。间隔 1～1440 分钟，默认全天 / 5 分钟 / 失败停用开启。
- 四项设置作为 options 表单个 JSON 值原子写入，每次选路和调度读取共享数据库，立即跨实例生效且重启保留，无新增表结构。GET /api/channel/astra_iq/settings 使用 ChannelRead，PUT 使用 ChannelWrite；沿用 AdminAuth、会话和操作审计。
- 用户明确要求“没有检测结果允许调用”。首次未测、删除最新结果/清空、凭据或版本变化后均允许调用；更早的历史记录不会被提升为最新结果。仅“失败停用”开启且最新适用结果为 fail/error 时拒绝 Astra 调用。关闭开关继续检测但不按结果停用；不改变普通渠道启用状态或其他模型。
- 时段外保留最后判定，取消原固定 10 分钟过期阻断；每日时段重新开始时启动首轮，全天模式跨午夜不额外启动。调度仍不重叠，轮询误差最多 15 秒。结束后不再开始新渠道/密钥的检测，在途请求可完成，未测完全部密钥的部分结果不覆盖判定。
- 客户端维持真实 Codex CLI 0.157.1 / Responses / medium，题目和答案 21 不变。

按用户“把没必要的测试删了”的要求，删除 6 项翻译/静态布局/通用键盘交互测试，将前端间隔输入校验保留一项代表性用例，范围和整数校验由真实后端接口验证，并删除与路由集成重复的后端状态矩阵。检测前端由 22 项精简至 14 项；保留历史选择和删除范围、错误重试、持久保存、权限、缓存路由即时生效及时间边界。

本地后端 SQLite 与 race 回归通过；精简后的前端 14 项通过，typecheck、变更文件 oxlint 和 oxfmt 通过。浏览器同组件预览中桌面 1280px、手机 390px 与 320px 暗色均无横向溢出，保存跨午夜时段后重开读取正确，零间隔显示校验；桌面含检测/不含检测卡片均高 230px。截图保留在本机 astra-iq-settings-{desktop,mobile,small-dark}.png。生产浏览器无登录会话，视觉验证使用同组件本地预览。

新增读写接口局部核对仍使用前文 OWASP ASVS 5.0.0 V8 的 8.1.1、8.2.1、8.2.2、8.3.1 与 Authorization Cheat Sheet，不改变身份验证或会话协议。发布与三数据库结果见后续部署记录。

### 本轮发布及部署结果

- 发布流水线 https://github.com/linjzy/new-api/actions/runs/36243943142 成功。SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19 均通过设置原子保存/非法参数拒绝/重新读取、内存与数据库路由、删除后无结果放行回归；race、前端 17 项（检测 14 项 + 日志 3 项）、typecheck、lint、构建、镜像启动及真实官方 CLI 集成均通过。
- 完整补丁应用到固定上游后，31 个实现文件逐字节一致；发布工具 11 项测试通过。实现提交 8d3d76cf，发布输入提交 2bf437377ea41734a5bff7d4f5192d17cc0cc197。
- 镜像：ghcr.io/linjzy/new-api@sha256:d2b7425b3ba8c0075d14eed93baa5e78f0a36df16f93008601c99c851fce470a。
- 源码分支：custom/1.0.0-rc.40-custom-f90568479205-0aec08fee811；源码提交 b362d61689037ab2a04690c91bf8fb9a0db1dbaf。
- 补丁摘要：7c722de13d8b4171122b32803dd853d0d36c55c22d2b322de5088161c0d85503。
- 2026-09-26 21:15:01（北京时间）切换到新镜像，21:15:02 部署任务退出码 0，耗时 36 秒。容器 healthy，维护门闩关闭，本机及公网 /api/status 均成功，公网首页 HTTP 200；设置 GET/PUT 的未认证访问均返回 HTTP 401，镜像内 codex --version 为 codex-cli 0.157.1。
- 上线前备份 /opt/new-api/backups/astra-iq-settings-20260926/，database.dump 为 8,108,402 字节，pg_restore --list 校验通过；Compose 和部署状态一并保留。
- 部署时生产未预设配置，使用默认全天 / 5 分钟 / 失败停用开启。收尾核查时数据库已保存新配置：00:00→00:00、1 分钟、失败停用开启；此为本轮上线后外部保存的配置，代理未改写，保留该最新值。未删除真实检测记录，也未为验证而改写真实判定。预览进程及工作树临时目录已清理，截图、测试日志和预览归档留在本机。

生产设置与自动检测验证：21:16:28 自动启动，21:17:07 完成；21:17:28 再次自动启动，符合最新 1 分钟配置。第二轮渠道 1 于 21:17:39 返回 21 / pass，耗时 11.686 秒；渠道 3 于 21:17:40 返回 21 / pass，耗时 12.373 秒；渠道 2 于 21:19:09 返回 error / request_failed，耗时 101.672 秒。三条历史均记录 Codex CLI 0.157.1 与 /v1/responses。第二轮在 21:18:28 仍运行时没有重叠新建任务，21:19:09 正常结束为 succeeded（任务完成，单渠道检测失败照常保留）。这验证了新版读取持久设置、动态间隔及官方客户端检测在生产实际工作。

## 失败详情、检测总开关与时间空档

用户要求显示渠道 1/2 的具体失败原因、增加总开关，并在关闭时隐藏卡片状态，未检测时段保持空白。已完成：

- 详情使用现有 Alert 展示失败原因、错误码、类型、HTTP 状态；答案错误明确显示预期 21 与实际答案。仅取解析后的错误字段，复用现有脱敏能力并去除已配置密钥、Header 覆盖值、Bearer、API key、JWT 等凭据，文本有界；不保存原始错误响应体。HTTP 200 流中报错也保留实际传输状态。
- 设置顶部“启用检测”默认开启；关闭后调度及逐密钥请求停止发起新调用、检测结果不阻断 Astra 路由、状态接口 enabled=false 使卡片条隐藏，历史保留。在途请求可完成。旧配置默认开启且保持已保存的时间/间隔。
- 状态条使用服务器时间和当前间隔显示最近 24 格，每格只放该时段实际检测记录，暂停、漏检、删除后的空档留白。新增 started_at 保存本轮开始时间，慢请求不会移动到后续完成时段；旧记录用 checked_at 减耗时兼容。未改数据库表结构。
- 状态条存在时分组限制 40% 宽度，关闭后恢复完整分组区域；卡片尺寸不变。

现场原因：2026-09-26 21:23:11 渠道 1 返回 HTTP 502 / Upstream access forbidden, please contact administrator；21:24:25 对应流错误为 The usage limit has been reached。渠道 2 多轮返回 The service is temporarily unavailable. Please retry later，21:25:54 返回 29 因预设答案 21 判错。渠道 1 的 21:25:29 记录已恢复为 pass / 21。以上根据对应时间历史与现场日志核对；没有改写旧结果。

在原测试文件扩展三项必要的前端回归（错误正文、旧记录降级、开关显隐及空档），合计 17 项通过。真实官方 CLI 集成补充 HTTP 502 与 HTTP 200 流失败，校验消息/错误码保留且密钥和原始 body 不泄漏；关闭总开关不产生上游请求；后端及 race 通过。旧 JSON 设置兼容、关闭后路由放行和状态隐藏加入现有三数据库测试。typecheck、变更文件 oxlint/oxfmt 通过。同组件预览桌面与 390px 手机无横向溢出，状态条关闭消失、开启恢复、缺测间隔留白；桌面卡片均高 230px，失败原因显示在题目前。预览截图和日志保留，本地临时预览进程已关闭。

### 总开关版本部署结果

流水线 https://github.com/linjzy/new-api/actions/runs/36245990178 成功，包含 SQLite 3.50.4 / MySQL 8.0.46 / PostgreSQL 15.19、race、真实 Codex CLI、前端 20 项、typecheck、lint、构建与镜像启动。实现 d062a597，发布输入 3f266117。

2026-09-26 21:50:09（北京时间）部署完成，退出码 0，耗时 15 秒，容器 healthy，维护门闩解除。镜像 ghcr.io/linjzy/new-api@sha256:aa392560b5d03b2d99a38c37a5a3d2cb5ca9f23ffb28d5c9ddc13304b7069461；源码 custom/1.0.0-rc.40-custom-a3b2d0493db6-0aec08fee811 / 98e5ba537cde443e0c27c71ee59a5780acf6b835；补丁摘要 dbd7f6a37ee67bf86b5c4fdd283d8790f468fd60e0f4f9cfdfad5465829674d3。备份 /opt/new-api/backups/astra-iq-errors-switch-20260926/，dump 8,117,013 字节，pg_restore --list 校验通过。

本地和公网 /api/status 均成功，首页 HTTP 200，未认证 GET/PUT 设置 HTTP 401。保留原有全天 / 1 分钟 / 失败停用配置，旧 JSON 缺少 enabled 时默认开启。21:52:50 自然启动的检测，渠道 1/2/3 均 pass / 21，生产历史含 started_at，真实客户端 Codex CLI 0.157.1。

## 连续检测误留白修复

用户截图显示不停检仍出现空白。现场任务存在 60～75 秒的常规启动间隔及其他渠道慢请求导致的整体顺延；原整分钟分桶错误地将这些情况视为漏检。状态接口改为只附带最近 24 个实际任务的开始/完成时间，前端每轮占一格，按整轮耗时和轮询容差识别真正缺测。删除结果保留该轮空位，暂停超过计划轮次继续留白；不改调度频率、题目、判分、普通调用或卡片尺寸。任务 payload/error 等内部信息不返回。

先增加失败回归复现，再修复。前端现有文件内三组必要案例覆盖轮询漂移和其他渠道长耗时、真实暂停、删除结果；共 20 项通过。后端原三数据库用例增加实际轮次返回及内部数据不泄漏验证；本地 SQLite / controller / router 通过，typecheck、lint 和格式通过。生产只读时间数据在同组件预览回放：渠道 1/3 的 24 条连续记录均连续显示，无内部空档；渠道 2 留下的 9 条记录连续显示，此前已无记录的位置仍留白。1280px / 390px 视口均无横向溢出，卡片高度 230px。

用户确认渠道 2 上游提示客户端断开对应 21:54:50 和 21:59:25 两条。生产保存错误均为 HTTP 200 / bad_response / response stream ended before completion，耗时分别 60.524 秒、50.561 秒；两次服务日志的 stream ended 原因均为 eof，未收到终结事件，无本地 timeout/client_gone。检测总时限及 CLI 空闲时限为 120 秒，服务流时限为 300 秒，RELAY_TIMEOUT 未设置（0），渠道无额外代理。现有证据表明这两次先收到上游链路 EOF，再由网关清理连接；不支持归因为检测器主动超时。上游服务与中间转发层谁先关闭还需上游更底层日志，不能仅凭“客户端断开”标签断言归属。

### 连续检测修复发布结果

- 实现 effd00f0，发布输入 164ddace。流水线 https://github.com/linjzy/new-api/actions/runs/36247454982 成功；SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19 均通过轮次读取回归，前端 23 项（检测 20 + 日志 3）、typecheck、lint、race、真实官方 CLI、构建与镜像启动通过。完整补丁无 fuzz 应用后，31 个实现文件逐字节一致。
- 镜像 ghcr.io/linjzy/new-api@sha256:219d8cb2e2a16353f11911b3df03d356dae990559d9658eb7bda3c8fcfa8176d；源码 custom/1.0.0-rc.40-custom-24e14c29d4d0-0aec08fee811 / ed62330133f77600a2f8165495118400ef229c32；补丁摘要 8e76a5e331c668a7fa7d7057ec14eca5c26f70ea0cb2be22db6b70a4eb104b53。
- 2026-09-26 22:18:09（北京时间）切换，22:18:10 部署退出码 0，耗时 9 秒。切换前检测轮次已结束且 8899 活跃连接为 0。上一轮 22:17:20→22:17:36 成功，新版自然首轮 22:18:21→22:18:39 成功，未打断检测。应用与 PostgreSQL healthy、Redis running、维护门闩解除，公网 API/首页 HTTP 200，状态接口未认证 HTTP 401，镜像客户端 codex-cli 0.157.1。
- 备份 /opt/new-api/backups/astra-iq-timeline-20260926/，dump 8,126,187 字节且 pg_restore --list 通过。保留用户全天 / 1 分钟 / 失败停用的设置，未删除或改写生产历史。预览进程和源码内临时目录已清理，脱敏时间快照、截图和日志保留本机。
- 两次流提前结束排查补充：故障期间容器从 21:50:01 持续运行、restart=0、OOM=false；21:53:50→21:54:50 后下一轮 21:55:05 启动，21:58:35→21:59:25 后下一轮 21:59:50 启动，未发生轮次重叠或部署中断。无证据支持本机主动取消；不以增大超时或自动重试掩盖缺失终结事件，也未改变检测判分。
