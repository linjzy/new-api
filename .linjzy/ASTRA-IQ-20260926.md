# Astra 官方客户端检测升级验证

任务：每 5 分钟检测启用渠道的 gpt-6-astra，推理强度 medium，糖果题正确答案 21；失败即时跳过路由，卡片底部纯色块可打开历史详情。

## 实现和维护

- 独立补丁：`patches/astra-iq-gate.patch`，基线 v1.0.0-rc.40 / `0aec08fee811ec6136828fda790551b49e410301`。应用完整补丁后与实现工作区逐文件比较一致。
- 真实官方 Codex CLI 0.157.1 随镜像安装，官方发布包固定摘要；每次调用使用临时目录、只读沙箱和一次性本地代理，只有 `/v1/responses` 的一次请求可访问指定渠道。
- 保留既有渠道转换、模型映射、medium 参数覆盖、检测日志和用量逻辑。CLI 只持有临时代理令牌，不持有上游密钥。
- 详情复用 `components/dialog.tsx`、`loading-state.tsx`、`error-state.tsx`、`ui/button.tsx` 和 `ui/tooltip.tsx`。离散历史色块需要独立点击目标，现有 Progress 不支持此行为，因此新增业务时间线组合组件。
- 历史列扩容为跨数据库 LongText，保留原数据；旧记录没有保存的耗时和 Token 数不会补造。

## 本地验证

- `bash .linjzy/bin/verify-release.sh /Users/linjzy/new-api-astra-iq go`：通过。
- `bash .linjzy/bin/verify-release.sh /Users/linjzy/new-api-astra-iq race`：通过。
- 最终代理验证追加后：`go test -race ./controller -run '^TestCustomAstraIQ' -count=1`：通过。
- `bun run test src/features/channels/components/__tests__`：10 个文件、208 项通过。
- `bun run typecheck`、修改文件 oxlint 与 oxfmt 检查、`bun run build`：通过。
- `python3 -m unittest discover -s .linjzy/tests -p 'test_*.py'`：11 项通过。
- 发布脚本及 CLI 安装脚本 shellcheck：通过。
- 桌面、390px 手机、暗色主题实测；点击历史结果、Escape 关闭和弹窗滚动正常，无横向溢出。生产控制台当前浏览器会话未登录，视觉验证基于同一组件的本地预览。

## 安全边界核对

参照 [OWASP ASVS 5.0](https://github.com/OWASP/ASVS/tree/v5.0.0_release/5.0/en)、[Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)、[Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)。本次局部检查覆盖 ASVS 7.2.1–7.2.3 的服务端验证、动态令牌和强随机性，以及 7.4.1 的失效后拒绝使用；不代表整站 ASVS 认证。

本地令牌通过 crypto/rand 生成，常量时间比较，仅绑定回环地址，最多存活单次检测的 2 分钟，拒绝空/错误令牌、错误方法和路径及重复使用；对应回归已通过。临时文件在调用完成后删除，不保存 CLI 原始错误输出。历史详情仅使用已有渠道读取权限，响应不包含渠道密钥或指纹。

## 渠道 1 排查

升级前多轮检测返回 HTTP 403，原因：上游仅允许 Codex 官方客户端。2026-09-26 在 179 直接执行官方 CLI，gpt-6-astra / medium 约 11.13 秒返回 21。随后将真实 CLI 纳入定时检测，并修复了该上游的流式结果兼容问题；最终部署后渠道 1 返回 21，通过检测，详见下方记录。

## 最终发布和部署

首次升级验证流水线：https://github.com/linjzy/new-api/actions/runs/36238921298

流式兼容修复流水线：https://github.com/linjzy/new-api/actions/runs/36239892140

生产数据库及 Compose 备份：`/opt/new-api/backups/astra-iq-official-20260926/`。

最终流水线成功。数据库版本 SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19，均运行新建表、旧表迁移、两次启动迁移、大于 64KB 的历史保存、内存和数据库路由跳过/恢复验证；命令为发布包 `verify-release.sh SOURCE go`，并追加 `race`。

首次升级镜像：`ghcr.io/linjzy/new-api@sha256:c1470395ec4f5dfcc4eb9e18b694403923cf7961a2f215566c36095f4e35e94a`。

首次升级源码：`custom/1.0.0-rc.40-custom-96802322a014-0aec08fee811`，提交 `2ba26e72fad687a9252b23c84d4550827c66772c`。

2026-09-26 19:38（北京时间）启用新镜像，容器健康。渠道 1 首轮自动检测 19:38:11 返回 21 / HTTP 200，耗时 8.806 秒，首字 7.763 秒；历史详情保存了 Codex CLI 0.157.1 与 /v1/responses。

首轮渠道 3 于 19:38:15 回答 21 通过；渠道 2 于 19:38:36 回答 29，状态 fail / wrong_answer，按规则排除。三条渠道消费日志均为 medium 和 /v1/responses。部署任务结束码 0，耗时 15 秒，维护门闩已解除，容器 healthy，公网 /api/status HTTP 200 / success=true。

临时独立 CLI 安装包和二进制已清理；正式 CLI 保存在发布镜像内。临时构建工作树、复制的验证文件和预览进程已清理，保留预览截图和验证日志。数据库备份及上一镜像保留以便回滚。

19:43:02 自然触发第二轮，距上一轮开始 303 秒，调度符合 5 分钟 + 轮询误差。现场发现渠道 1 的部分 Responses 流在 output_item.done 中完整给出 21，但 response.completed 的 output 为空；原解析器将其误判为异常。已先增加失败用例复现，再允许成功完成响应使用已完成的 assistant 消息；未完成流与 commentary 仍不能通过。真实 CLI 集成、判分与 race 回归均通过。

### 最终流式兼容修复

- 最终流水线 `36239892140` 状态 success，包含三种数据库、race、前端发布检查、官方 CLI 集成和镜像启动验证。
- 最终镜像：`ghcr.io/linjzy/new-api@sha256:135dcc507b4f09b06c417a9d8273f7c3cca9f53ead2c82ee5bdf29d9faa31e44`。
- 最终源码：`custom/1.0.0-rc.40-custom-6db358a862ab-0aec08fee811`，提交 `95196c54eda4571261f104b8d8c57180f4a8ecd9`。
- 最终补丁摘要：`b5726ed8c11abfb74a340c994e5d8e4d84e178b38d5c7b69f643fd002a5e9b8e`。
- 2026-09-26 19:55:52（北京时间）部署完成，任务退出码 0，耗时 14 秒；维护门闩已解除，容器 healthy，公网 `/api/status` 返回 HTTP 200 / success=true。

部署后立即通过正常任务队列触发一轮官方客户端检测，结果如下。渠道启用状态保持原值，检测结果来自真实请求，没有手工改写判定。

| 渠道 | 完成时间（北京时间） | 回答 | 判定 | HTTP | 总耗时 | 首字耗时 | 输入 / 输出 Token |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 / openai-nex | 19:56:02 | 21 | pass | 200 | 13.695 秒 | 12.373 秒 | 12836 / 326 |
| 2 / openai-bot | 19:56:43 | 29 | fail / wrong_answer | 200 | 55.244 秒 | 2.518 秒 | 12834 / 518 |
| 3 / openai-krill | 19:56:04 | 21 | pass | 200 | 15.706 秒 | 3.012 秒 | 8682 / 382 |

以上三条历史均记录 `Codex CLI 0.157.1` 和 `/v1/responses`，对应消费日志均记录 medium。检测任务最终状态 succeeded。渠道 1 本轮真实流仍是 `response.output_item.done` 给出 completed / final_answer / 21，而 `response.completed` 为 completed / output=[]；新解析逻辑正确取得已完成答案，并与官方 CLI 完成结果一致，确认该兼容修复在生产实际生效。

历史中的旧黄色结果继续保留，最新检测按每轮结果显示；渠道 2 在这一轮按既定答案 21 的规则暂不参与 gpt-6-astra 路由。

20:00:48 自动开始下一轮，距前一轮开始 302 秒。渠道 2 于 20:01:09 回答 21，自动恢复路由；渠道 3 于 20:00:57 回答 21。渠道 1 于 20:01:20 请求失败，耗时 31.664 秒；同一请求的服务端流处理日志为 `handler_stop` / `Upstream service temporarily unavailable`，只收到 1 条事件。这属于上游服务暂不可用，与此前的 HTTP 403 客户端限制和空 terminal output 解析问题不同；按既定规则记录黄色并跳过该渠道，未将异常结果改写为通过。

20:06:03 下一轮自动开始，20:06:34 成功结束。渠道 1 已恢复正常响应，HTTP 200，但回答 29，按预设答案 21 判为 fail / wrong_answer（红色），耗时 27.651 秒；渠道 2 回答 29，fail，耗时 30.950 秒；渠道 3 回答 21，pass，耗时 12.868 秒。三条记录仍为真实官方 CLI / Responses 检测。最终确认客户端限制和空 terminal output 的误判已修复，真实上游故障和错误答案仍如实记录并控制路由，旧历史不被覆盖。

## 同行布局与历史删除（后续调整）

检测色块移动到分组名称后，同一行显示，不增加卡片高度。复用原 GroupBadge、AstraIQStatus、Dialog、ConfirmDialog 和 Button，没有新增基础交互封装。

在同组件预览中对比有/无检测条的相同卡片：1280px 和 390px 视口下均高 230px，320px 视口下均高 250px；单分组、多分组、长名称、无分组、停用渠道均无横向溢出，色块和分组垂直对齐。分组最多使用 40% 行宽，长名称沿用省略及悬停完整名称，剩余空间显示检测色块。

详情新增“删除此次结果”和“删除全部结果”。全部范围经用户确认仅为当前渠道；确认框明确范围。旧记录删除不改变最新判定；最新记录被删除或清空后，当前状态变为 pending，旧通过记录不恢复路由资格，下一次检测生成新结果。

- `DELETE /api/channel/astra_iq/:id/:checked_at`：删除指定时间记录，不存在返回 404，无效标识返回 400。
- `DELETE /api/channel/astra_iq/:id`：清空当前渠道，重复清空可安全重试。
- 两接口复用 AdminAuth 和 ChannelWrite 服务端权限校验及现有操作审计；只有渠道读取权限的页面不展示删除操作。
- 写入与删除使用 GORM 事务和现有 lockForUpdate 跨数据库封装，避免定时写入使用删除前的历史副本；不变更数据库结构。
- 参照 [OWASP ASVS 5.0.0 V8](https://github.com/OWASP/ASVS/blob/v5.0.0_release/5.0/en/0x17-V8-Authorization.md) 的 8.1.1、8.2.1、8.2.2、8.3.1，以及 [Authorization Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)，复用既有身份校验和会话机制。此为本次新增端点局部核对，不代表整站认证。

本地 `go test -race ./controller ./router -run '^TestCustomAstraIQ' -count=1` 通过；前端相关 15 项测试、typecheck、oxlint、oxfmt 通过。浏览器验证取消不删除，单次删除 24→23，清空 23→0，其他渠道仍为 24；网络错误和业务错误均保留历史、展示失败信息并允许重试。完整补丁可应用到固定上游，28 个补丁文件与实现源码一致；发布工具 11 项测试通过。

发布流水线：https://github.com/linjzy/new-api/actions/runs/36241979880 ，最终状态 success。SQLite 3.50.4、MySQL 8.0.46、PostgreSQL 15.19 均通过新增的删除/清空/后续写入/路由撤销回归；CI 前端 18 项测试通过，后端 race、typecheck、lint、构建、镜像启动和官方 CLI 检查通过，发布脚本 shellcheck 通过。

2026-09-26 20:37:22（北京时间）部署完成，退出码 0，耗时 35 秒。新镜像 `ghcr.io/linjzy/new-api@sha256:e155af55d650c246511d7ed785cfe094d0553c74d8efd4255c36a1b98e0a50a6`，源码 `custom/1.0.0-rc.40-custom-c7f43625e3bc-0aec08fee811` / `54e78d3ec83e0400b820f9bb93d94cf36d0fe4b9`，补丁摘要 `7412eaee78b8f2c515e3a9f29b43354e17c31a33bea9816e50fd1b3597b82489`。

生产容器 healthy，维护门闩解除；本地和公网 `/api/status` 均 HTTP 200 / success=true，两个删除端点的未认证请求均 HTTP 401，镜像内 `codex --version` 为 0.157.1。真实删除行为在隔离数据库和组件预览验证。上线前数据库及 Compose 备份保留在 `/opt/new-api/backups/astra-iq-history-20260926/`，dump 8,102,281 字节且已通过 pg_restore --list 验证。临时预览进程和工作树内预览文件已清理，截图、日志与预览归档保留在本机。
